<div class="card shadow-sm border-0 w-50 mx-auto">
  <div class="card-header bg-light">
    <%= link_to back_to_list, class: 'btn' do %>
      <i class="fas fa-arrow-left"></i>
    <% end %>
    <h5 class="mb-0"><%= t('suppliers.title.create') %></h5>
  </div>

  <div class="card-body">
    <%= render 'form' %>
  </div>
</div>
